{
  "version": 1,
  "frontend": {
    "phases": {
      "build": {
        "commands": [
          "node -v",
          "npm -v",
          "npm ci || npm install",
          "npm run export",
          "ls -la out || (echo 'out missing - failing build'; exit 1)"
        ]
      }
    },
    "artifacts": {
      "baseDirectory": "out",
      "files": ["**/*"]
    },
    "cache": {
      "paths": ["node_modules/**/*"]
    }
  }
}
