version: 1
frontend:
  phases:
    build:
      commands:
        - npm ci || npm install
        - npm run export
        - ls -la out || (echo "out/ missing — failing build"; exit 1)
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
